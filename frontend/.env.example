# CRA embeds these values at build time. Use the public API origin only.
# Local default; set the Railway/preview/production origin in Vercel.
REACT_APP_BACKEND_URL=http://localhost:8000

# Supabase Auth (single Discord login). Set in Vercel for production.
# From Supabase dashboard: Project Settings -> API.
REACT_APP_SUPABASE_URL=https://mfsmqyxichkbnomvihmo.supabase.co
REACT_APP_SUPABASE_ANON_KEY=

# greek-bingo backend (stream games console). Public origin only.
# REACT_APP_BINGO_API_URL=