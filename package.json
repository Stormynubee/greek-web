{
  "name": "greek-web",
  "private": true,
  "version": "1.0.0",
  "description": "GreekGodBerry community website — Vercel root wrapper for the CRA frontend in /frontend.",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend start",
    "test": "npm --prefix frontend test"
  }
}
